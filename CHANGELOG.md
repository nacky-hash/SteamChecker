# CHANGELOG

このファイルは利用者向けの変更履歴です。
設計判断の理由は [docs/DECISIONS.md](docs/DECISIONS.md)、実測データは
[docs/RESEARCH.md](docs/RESEARCH.md)、作業の経緯は
[docs/STATUS.md](docs/STATUS.md) にあります。

## 未リリース

- `dry-run` で操作の開始を記録しないようにした。書き込みを伴わないため、
  途中で落とされても伝えるべきことがない
- `history` が開始レコードを一律「（未完了）」と表示していたのを、
  実際にまだ完了していないものだけに限定した

## v0.2.0-alpha — 2026-09-12

公開から 6 週間、実際に使って見つかった問題への対応。

### 追加

- **圧縮が中断されたことを次の起動時に伝えるようにした**（D-020）。
  メモリ不足などで OS にプロセスを落とされると、これまでは
  「突然消えた」としか分からなかった
- **空きメモリが下限を割ったら自分から中断する**ようにした（D-020）。
  落とされる前に止まれば理由を表示できる。`--memory-floor-mb` で下限を変更可能（既定 1 GiB）
- 実行前に**所要時間の目安**を表示するようにした。実測 23〜70 MB/s から範囲で出す
- 10 GB 以上のタイトルでは「中断されても安全」「再実行で続きから進む」ことを明示

### 変更

- **一度圧縮したタイトルは、過去の実測値を見込みの下限に使う**ようになった（D-021）。
  圧縮の到達点は決定的で、推定より確実なため
- **1 ファイルがフォルダの大半を占める構成での見込み精度を改善**した（D-022）。
  実圧縮 16 タイトルでの誤差は最大 12.0 pt → **4.9 pt**、±6 pt 超は 2 件 → **0 件**
- メモリ不足による中断の終了コードを `2` にした（通常の失敗 `1` と区別できる）

### 修正

- **`dry-run` の結果を「圧縮した」として記録していた。** そのため
  「もう縮まない」と誤判定したり、一括復元の対象に未圧縮フォルダが混ざる可能性があった
- 中断の通知ダイアログが、一覧の描画前に表示されていた（GUI）。
  真っ白な画面にダイアログだけが出る状態だった

### 確認したこと

- 3 つの成果物すべてを隔離環境で起動し、一覧 47 行の描画と
  `crash.log` が出ないことを確認（`tools/release_verify.ps1`）
- 実ゲーム（237 ファイル / 219 MB）で圧縮 → 復元を往復させ、
  **全ファイルが SHA-256 とサイズで完全一致**することを確認
- 107 GB のタイトルで中断 → 再実行 → 完走を確認
- テスト 149 → **165 件**、両ランナー（xUnit / 依存ゼロ）で成功

### 既知の制約（変わっていません）

- 無署名のため SmartScreen の警告が出る
- アンチチートは EasyAntiCheat で 1 タイトル確認したのみ。BattlEye 系は未確認
- 複数ライブラリ（D: や外付け）構成は未確認
- GUI でのメモリ監視の発火は未確認（空きが 1 GiB を割る状況を再現できないため）

## v0.1.0-alpha — 2026-07-30

最初の公開。Phase 0（解析のみ・書き込みなし）を既定とする。

- Steam ライブラリを走査し、タイトルごとに圧縮の可否と見込みを提示
- 圧縮率はコミュニティ製データベースではなく、**対象マシンの実データを読んで測る**
- WOF の実挙動に合わせ、32 KiB チャンク単位で圧縮率を推定
- 圧縮・復元の実行は `--experimental` によるオプトイン
- 事前検査（NTFS 判定、ジャンクション、Steam の起動状態、ファイルロック）
- 操作をすべて JSON Lines で記録

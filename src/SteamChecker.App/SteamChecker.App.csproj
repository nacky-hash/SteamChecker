<Project Sdk="Microsoft.NET.Sdk">

  <!--
    WPF 版。判定ロジックは一切持たない（AGENTS.md / D-009）。
    このプロジェクトの仕事は LibraryScanner.Scan() を呼んで
    AdviceFormatter / PreFlightFormatter の文言を並べることだけ。
    ロジックをここに書きたくなったら Core に書いてテストを付けること。
  -->

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <UseWPF>true</UseWPF>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>SteamChecker.App</RootNamespace>
    <AssemblyName>SteamChecker.App</AssemblyName>
    <ApplicationManifest>app.manifest</ApplicationManifest>

    <!--
      単一ファイル publish で WPF のネイティブ DLL（PresentationNative 等）も exe に同梱する。
      既定では exe の「隣」に置かれるため、exe 単体を配布すると起動時に
      DllNotFoundException で即死する（v0.1.0-alpha で実際に踏んだ）。
    -->
    <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
  </PropertyGroup>

  <ItemGroup>
    <!--
      空きメモリ取得の P/Invoke を CLI と共有する。
      Core に Windows 依存を置かない方針（AGENTS.md）のため Core には入れられず、
      かといって同じ P/Invoke を 2 箇所に書くのは避けたいのでソースを共有する。
    -->
    <Compile Include="..\SteamChecker.Cli\SystemMemory.cs" Link="SystemMemory.cs" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\SteamChecker.Core\SteamChecker.Core.csproj" />
  </ItemGroup>

</Project>

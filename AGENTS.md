# AGENTS.md — SteamChecker プロジェクトルール（正本）

このファイルがプロジェクトルールの**正本**。`CLAUDE.md` と矛盾した場合はこちらを優先する。

## 製品の定義

SteamChecker は、Windows の NTFS 透過圧縮（`compact.exe /EXE`）で Steam ゲームの
使用容量を削減する無料ツール。

**製品の中心は圧縮処理ではなく「どのゲームを圧縮すべきかの判断支援」。**

圧縮エンジンで CompactGUI と競争しない。「どれをどうすべきか教える」ことが役割。

## 絶対に守ること

### やってはいけない

- ゲームファイルの削除（提案はする。実行は絶対にしない）
- Steam 設定ファイルの書き換え
- Steam 認証情報の取得
- 外部サーバーへの送信 / テレメトリー / 広告 SDK / 自動更新
- 独自ドライバーの導入
- 管理者権限の常時要求
- CompactGUI 等 GPL コードのコピー
- 根拠のない「安全」「性能低下なし」という表示
- 「必ず N% 削減」のような一般化された効果の主張

### 表示・文言の規律

- 削減するのは **SSD/HDD 容量**。RAM ではない。RAM を訴求理由にしない
- `LastPlayed` が無いことを「一度も遊んでいない」と表示しない。「要確認」とする
- DirectStorage の兆候が無いことを「安全である」と表示しない
- 圧縮率は**実測に基づく推定値**として提示する。断定しない

## 開発の規律

- 既存コードを読まずに全面書き換えしない
- 修正理由が説明できない変更を加えない
- ユーザーの既存変更を勝手に破棄しない
- **テストを通すために安全チェックを弱めない**
- 警告や例外を握り潰さない
- **未確認事項を「確認済み」と書かない**
- テスト結果は実行したものと未実行のものを分けて書く
- Claude と Codex が同じファイルを同時編集しない

## 記録の義務

- 作業結果は `docs/STATUS.md` に追記する
- 設計判断を変えた場合は `docs/DECISIONS.md` に記録する
- 実測を行った場合は `docs/RESEARCH.md` に数値ごと残す

## アーキテクチャの制約

- **`SteamChecker.Core` に Windows 依存を持ち込まない。**
  判定ロジックは OS 非依存に保ち、Linux/CI でテストできる状態を維持する
- ファイルシステムは `IFileSystem` で抽象化する
- レジストリ参照はアプリ層からデリゲートで注入する
- WPF プロジェクトに判定ロジックを書かない

この制約のおかげで、判定ロジックのテストは OS を問わず全て通る（2026-08-01 時点で 162 件）。壊さないこと。

## 開発環境の実測値（2026-07-30 更新）

| 項目 | 状態 |
|---|---|
| .NET SDK | **10.0.302 導入済み**（2026-07-29 の「未導入」から更新） |
| PowerShell | **5.1 のみ**。pwsh 7 は無い。スクリプトは PS5.1 互換で書くこと（BOM 付き UTF-8 で保存） |
| 権限 | 既定は非管理者 |
| ドライブ | C: NTFS / E: exFAT（非 NTFS ブロックの実地テスト済み — RESEARCH §6 後段） |
| Steam | `C:\Program Files (x86)\Steam` — 45 タイトル / 441.9 GB |
